services:
  - type: web
    name: ziggo-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "10000"
      - key: PYTHON_VERSION
        value: "3.12.0"
    plan: free
    healthCheckPath: /health
